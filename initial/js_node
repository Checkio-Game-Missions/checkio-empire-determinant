"use strict";
function golf(matrix) {
    return 0;
}
//
//var assert = require("assert");
//if (!global.is_checking) {
//    // These "asserts" using only for self-checking and not necessary for auto-testing
//    assert.equal(golf([[4, 3], [6, 3]]), -6, "First");
//    assert.equal(golf([
//        [1, 3, 2],
//        [1, 1, 4],
//        [2, 2, 1]]), 14, "Second")
//}